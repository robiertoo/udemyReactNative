import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    textoGrande: {
        fontSize: 32,
        textAlign: 'center',
    },
    caixaTexto:{
        borderColor: 'black',
        borderWidth: 0.1,
        padding: 5,
        textAlign: 'center',
        borderRadius: 50
    },
});